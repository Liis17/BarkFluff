name: Сборка и отправка в регистр Beacon и GHCR
on:
  push:
    branches:
      - dev
    paths:
      - 'Backend/BarkFluff.Beacon/**'
      - 'Backend/BarkFluff.GrpcServer/**'
      - '.github/workflows/beacon-build-dev.yml'
jobs:
  build-beacon:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      
      - name: Login to Private Registry
        uses: docker/login-action@v2
        with:
          registry: docker.barkfluff.com
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}
      
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Build and Push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Backend/BarkFluff.Beacon/Dockerfile
          push: true
          tags: |
            docker.barkfluff.com/barkfluff-beacon-dev:latest,
            docker.barkfluff.com/barkfluff-beacon-dev:${{ github.sha }},
            ghcr.io/${{ github.actor }}/barkfluff/barkfluff-beacon-dev:latest,
            ghcr.io/${{ github.actor }}/barkfluff/barkfluff-beacon-dev:${{ github.sha }}