port: 7880

rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50200
  use_external_ip: true

keys:
  # Заполните после Setup UI той же парой, что и Calls:LiveKit:ApiKey/ApiSecret.
  # Пустая конфигурация намеренно не даёт использовать общий development-secret.
  {}
webhook:
  api_key: ""
  urls:
    - http://calls:7026/livekit/webhook
