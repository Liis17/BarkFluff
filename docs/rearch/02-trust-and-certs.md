# 02 — Модель доверия, сертификаты и подпись S2S-трафика

## Требования (из постановки задачи)

1. Бэкенды нод общаются между собой **зашифрованно**.
2. **Зависимость от центров сертификации недопустима**: CA может отозвать сертификат конкретной ноды и выключить её из сети. Админ каждой ноды должен иметь возможность сделать свой сертификат сам (self-signed).

## Два независимых слоя

### Слой 1 — транспортное шифрование: TLS (допускается self-signed)

- Весь S2S-трафик — gRPC поверх TLS (HTTP/2). Терминация — на nginx ноды (как сейчас для клиентского трафика).
- Сертификат может быть любым: Let's Encrypt, покупной, **self-signed**. Проверка цепочки CA у S2S-клиента **отключена** (custom `RemoteCertificateValidationCallback`).
- Чтобы self-signed не превращался в «шифруем неизвестно с кем» (MITM), применяется **SPKI-пиннинг**: отпечаток публичного ключа TLS-сертификата ноды публикуется в discovery-документе (`/.well-known/barkfluff`, Navigator, ручная запись админа) и фиксируется в KnownServers. S2S-клиент сверяет отпечаток предъявленного сертификата с закреплённым.
  - Смена TLS-сертификата (плановая ротация) — через обновление discovery-документа: подпись документа signing-ключом (слой 2) делает обновление доверенным.
  - Первый контакт с нодой, у которой нет записи в KnownServers — bootstrap через discovery по DNS: отпечаток берётся из `/.well-known`. **Bootstrap-фетч well-known выполняется только по CA-валидному HTTPS** — иначе первый контакт не защищён вовсе: MITM на пути к apex-домену подсовывает документ со своими ключами и своим SPKI-пином, и вся дальнейшая «доверенная цепочка обновлений» строится на ключах атакующего. Компромисс фиксируем явно: CA-серт нужен *только* для bootstrap-канала discovery; весь S2S, ротации и работа с уже знакомыми пирами (KnownServers) от CA не зависят. Отзыв apex-серта не выключает ноду из сети — лишь мешает новым знакомствам, а они закрываются ручным пиром ([03-discovery.md](03-discovery.md), источник 3). Когда при первом контакте доступны и well-known, и Navigator — кросс-сверка ключей из обоих источников **обязательна** (расхождение → отказ + алерт).

### Слой 2 — подлинность: Ed25519 signing key ноды

Транспортный TLS (тем более self-signed) не доказывает, что запрос пришёл именно от `chat.example.org`. Подлинность обеспечивается прикладными подписями — по образцу Matrix.

- При первичной настройке ноды генерируется пара Ed25519 (**signing key**). Приватный ключ хранится у Federation-сервиса (см. вопрос хранения ниже), публичный — публикуется через все каналы discovery.
- Ключи версионируются: `key_id` (например `ed25519:1`). Нода может иметь несколько активных ключей (для ротации) и список отозванных с `expired_at`.

### Подпись каждого S2S-запроса

Каждый federation-RPC несёт metadata-заголовок (по аналогии с существующим XAuth):

```
x-bf-origin:      chat.example.org          # имя ноды-отправителя
x-bf-destination: barkfluff.com             # имя ноды-получателя (защита от relay)
x-bf-timestamp:   1789000000000             # unix ms UTC
x-bf-key-id:      ed25519:1
x-bf-signature:   base64(Ed25519-подпись канонической строки)
```

Каноническая строка подписи:

```
{origin}\n{destination}\n{timestamp}\n{grpc-method-full-name}\n{sha256(request-bytes)}
```

`request-bytes` — это **полученные wire-байты** сообщения, как они пришли по сети, до protobuf-десериализации. Protobuf-сериализация не канонична между реализациями (порядок полей/map, unknown fields), поэтому проверяющая сторона не имеет права пере-сериализовывать запрос — она хеширует именно принятые байты (в ASP.NET Core gRPC — кастомный marshaller / перехват на уровне байтов; зафиксировать в спецификации протокола для сторонних реализаций). Конверт `SignedRequest { bytes payload }` рассмотрен и **отвергнут**: он уничтожает типизацию gRPC-контракта и ломает стриминговые методы. **Уточнение этапа 1.3**: все S2S-RPC v1 имеют унарные *запросы* (стримы только в ответах — `FetchFile`, `SubscribePresence`), поэтому подпись покрывает wire-байты запроса целиком тем же механизмом, что и любой другой RPC; случая client-streaming в протоколе v1 нет.

Получатель (Federation-сервис ноды-адресата):

1. Проверяет `destination == моё имя` (иначе запрос переслан/зеркалирован — отказ).
2. Проверяет `|now - timestamp| <= окно` (рекомендация: 5 минут) — анти-replay. Отдельный nonce-кеш не нужен: все мутации идемпотентны по `event_id`.
3. Достаёт публичный ключ `origin` + `key_id` из KnownServers (при отсутствии — discovery на лету).
4. Проверяет подпись. Ошибка → `Unauthenticated`, инкремент метрики, запись в лог.
5. Проверяет, что `origin` не в блоклисте ноды.

Внутри тела события все ссылки на «кто автор» дополнительно валидируются правилом: **нода может говорить только за пользователей своего домена** (`sender.server_name == x-bf-origin`).

### Почему не mTLS

mTLS + пиннинг клиентских сертификатов решал бы обе задачи разом, но:
- усложняет прохождение через nginx/промежуточные прокси (нужен passthrough или проброс клиентского серта);
- привязывает подлинность к TLS-слою — при смене серта рвётся доверие;
- подписи уровня приложения дают доказуемое авторство конкретного события: каждый `FederationEvent` несёт собственную подпись origin (`origin_signature`/`origin_key_id`, см. [04-federation-service.md](04-federation-service.md)) — копия чата хранит её и может быть перепроверена позже, в том числе для истории, полученной через catch-up.

Ed25519-подписи + пиннинг TLS-ключа покрывают требования и не зависят от CA. mTLS можно добавить позже как опциональное усиление между дружественными нодами.

## Хранение приватного ключа

Варианты:
1. **Configuration-сервис** (как остальные секреты — `Federation:SigningKeyPrivate`). Просто, вписывается в текущую схему, но секрет лежит в общей конфиг-БД.
2. Файл на диске Federation-контейнера (volume) + путь в конфигурации.
3. Секрет-менеджер — вне текущего стека, избыточно.

**Принято** (этап 1.2, см. [phase-1/step-1.2-keys-wellknown.md](phase-1/step-1.2-keys-wellknown.md)): не вариант 1, а `FederationDb`, таблица `SigningKeys` — ключ не покидает Federation, ротация всё равно требует структурного хранилища, обратный канал записи в Configuration не нужен. MVP — без шифрования приватного seed; вынос в защищённое хранилище остаётся бэклогом.

## Ротация и компрометация ключей

- **Плановая ротация**: добавить новый `key_id`, публиковать оба, подписывать новым; старый пометить `expired_at` спустя перекрытие (например 30 дней). Ноды-партнёры обновляют ключи при периодическом рефреше discovery (рекомендация: раз в сутки + по требованию при неизвестном `key_id`).
- **Компрометация**: админ генерирует новый ключ, помечает старый отозванным в discovery-документе и **push-рассылает `KeyRevoked`-событие всем KnownServers** — не ждать суточного рефреша, иначе украденный ключ принимается пирами до суток после отзыва. Важно: окно `timestamp` защищает только от **replay** старых запросов — свежим запросам, подписанным украденным ключом, оно не мешает, и смягчением компрометации не является. Остаточный риск принят явно: при компрометации *единственного* ключа ноды атакующий (контролируя канал к well-known) может публиковать собственный «валидный» discovery-документ, и отличить его от документа админа пиры не могут — восстановление доверия в этом случае только внеполосно (ручное перезнакомство пиров). Офлайновый master-ключ, подписывающий ротации, — возможное усиление вне MVP ([09-problems-open-questions.md](09-problems-open-questions.md), №8).
- Discovery-документ сам подписывается signing-ключом → цепочка обновлений доверенная после первого знакомства.

## Что нового появляется в системе

| Артефакт | Где |
|----------|-----|
| Генерация/хранение Ed25519-ключей, ротация | `BarkFluff.Federation` |
| Подпись исходящих S2S-запросов (client interceptor) | `BarkFluff.Federation` (аналог `JwtClientInterceptor`) |
| Проверка подписей входящих (server interceptor / middleware) | `BarkFluff.Federation` (аналог XAuth, «XFed») |
| SPKI-пиннинг TLS в S2S gRPC-клиенте | `BarkFluff.Federation` |
| Публикация публичных ключей | `/.well-known/barkfluff` (nginx/WebServer), Navigator, AdminPanel |
| Управление доверием (просмотр ключей пиров, блок ноды, ручной пир) | AdminPanel |

## Проблемы и открытые вопросы

- **Часы**: окно `timestamp` требует NTP-синхронизации нод. Рассинхрон > окна = отказ в обслуживании ноде с кривыми часами. Диагностика: специальный код ошибки `ClockSkewDetected` с серверным временем в ответе.
- **Первый контакт**: bootstrap-фетч well-known — только по CA-валидному HTTPS (см. слой 1); кросс-сверка well-known ↔ Navigator при первом контакте обязательна, когда оба источника доступны; ручной пир — канал знакомства, не зависящий от CA вовсе. Логирование смены ключей.
- **Отзыв доверия ноде** — только локальное решение каждого админа (блоклист) + возможная модерация каталога Navigator. Глобального отзыва нет by design.
- ~~Выбор криптобиблиотеки~~ — **решено**: `BouncyCastle.Cryptography` 2.6.2 (managed, без нативных зависимостей — снимает риск chiseled-контейнеров конструктивно). BCL .NET 10 плоского Ed25519 не содержит (только как компонент гибридных Composite ML-DSA схем). Подробности, бенчмарки, RFC-векторы — [step-0.5-report.md](phase-0/step-0.5-report.md).
