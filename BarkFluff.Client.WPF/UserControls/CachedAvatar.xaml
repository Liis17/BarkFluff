<UserControl
    x:Class="BarkFluff.Client.WPF.UserControls.CachedAvatar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    x:Name="MainAvatar"
    d:DesignHeight="50"
    d:DesignWidth="50"
    mc:Ignorable="d">
    <Grid>
        <Border
            x:Name="AvatarBorder"
            CornerRadius="25"
            RenderOptions.BitmapScalingMode="HighQuality">
            <Border.Background>
                <ImageBrush
                    x:Name="AvatarBrush"
                    ImageSource="pack://application:,,,/BarkFluff;component/Resources/Placeholders/userplaceholder.png"
                    RenderOptions.BitmapScalingMode="HighQuality"
                    Stretch="UniformToFill" />
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect
                    x:Name="ShadowEffect"
                    BlurRadius="12"
                    Opacity="0.9"
                    ShadowDepth="0"
                    Color="Gray" />
            </Border.Effect>
        </Border>

        <Border
            x:Name="SavedChatAvatar"
            Width="50"
            Height="50"
            Panel.ZIndex="2"
            Background="#508fd6"
            CornerRadius="25"
            Visibility="Collapsed">
            <ui:SymbolIcon
                x:Name="SavedChatIcon"
                Width="30"
                Height="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Filled="False"
                FontSize="30"
                Foreground="White"
                Symbol="Bookmark32" />
        </Border>

        <Border
            x:Name="UserWithoutAvatar"
            Width="50"
            Height="50"
            Panel.ZIndex="3"
            Background="#508fd6"
            CornerRadius="25"
            Visibility="Visible">
            <ui:SymbolIcon
                x:Name="UserIcon"
                Width="30"
                Height="30"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Filled="True"
                FontSize="30"
                Foreground="White"
                Symbol="Person32" />
        </Border>
    </Grid>
</UserControl>
