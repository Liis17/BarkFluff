<UserControl
    x:Class="BarkFluff.Client.WPF.UserControls.CropImage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:BarkFluff.Client.WPF.UserControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Width="500"
    Height="600"
    MaxWidth="500"
    MaxHeight="600"
    d:DesignHeight="600"
    d:DesignWidth="500"
    AllowDrop="True"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Storyboard x:Key="FadeIn">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                From="0"
                To="1"
                Duration="0:0:0.25" />
        </Storyboard>
        <Storyboard x:Key="FadeOut">
            <DoubleAnimation
                Storyboard.TargetProperty="Opacity"
                From="1"
                To="0"
                Duration="0:0:0.25" />
        </Storyboard>
    </UserControl.Resources>
    <Grid Background="#1e1e1e">
        <Grid x:Name="ButtonGrid">
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Orientation="Vertical">
                <ui:Button
                    Click="LoadButton_Click"
                    Style="{StaticResource MainButton}"
                    ToolTip="Выбрать картинку для аватара в проводнике">
                    <StackPanel Orientation="Horizontal">
                        <ui:TextBlock Style="{StaticResource MainButtonTextLeft}" Text="Выбрать аватар" />
                        <ui:SymbolIcon Style="{StaticResource ButtonSymbolRight}" Symbol="ArrowDownload20" />
                    </StackPanel>
                </ui:Button>
                <TextBlock
                    Margin="0,10,0,0"
                    HorizontalAlignment="Center"
                    Foreground="#888888"
                    Text="или перетащите файл сюда" />
            </StackPanel>
        </Grid>
        <Grid x:Name="CropGrid">
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Bottom"
                Panel.ZIndex="100"
                Orientation="Vertical">
                <StackPanel HorizontalAlignment="Center" Orientation="Horizontal">
                    <ui:Button
                        Click="Button_Click"
                        Style="{StaticResource MainButton}"
                        ToolTip="Выбрать текущий вариант аварата и применить его для профиля">
                        <StackPanel Orientation="Horizontal">
                            <ui:TextBlock Style="{StaticResource MainButtonTextLeft}" Text="Обрезать" />
                            <ui:SymbolIcon Style="{StaticResource ButtonSymbolRight}" Symbol="Cut20" />
                        </StackPanel>

                    </ui:Button>
                    <ui:Button
                        Click="ResetPosition"
                        Style="{StaticResource MainButton}"
                        ToolTip="Вернуть картинку в исходное состояние">
                        <ui:SymbolIcon
                            Margin="25,0,25,0"
                            Style="{StaticResource ButtonSymbolSingle}"
                            Symbol="ArrowMoveInward20" />
                    </ui:Button>
                    <ui:Button
                        Click="LoadButton_Click"
                        Style="{StaticResource MainButton}"
                        ToolTip="Выбрать другую картинку">
                        <ui:SymbolIcon
                            Margin="25,0,25,0"
                            Style="{StaticResource ButtonSymbolSingle}"
                            Symbol="ImageArrowCounterclockwise20" />
                    </ui:Button>
                    <ui:Button
                        x:Name="CancelButton"
                        Click="CancelButton_Click"
                        Style="{StaticResource MainButton}"
                        ToolTip="Отменить и вернуться к выбору файла">
                        <ui:SymbolIcon
                            Margin="25,0,25,0"
                            Style="{StaticResource ButtonSymbolSingle}"
                            Symbol="Dismiss20" />
                    </ui:Button>
                </StackPanel>
                <Slider
                    x:Name="ZoomSlider"
                    Width="300"
                    Margin="10"
                    HorizontalAlignment="Center"
                    Maximum="2.5"
                    Minimum="0.5"
                    TickFrequency="0.05"
                    TickPlacement="BottomRight"
                    Value="1.0" />
            </StackPanel>
            <Grid
                Height="200"
                Margin="-1"
                VerticalAlignment="Bottom"
                Panel.ZIndex="10">
                <Grid.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Offset="0" Color="#00000000" />
                        <GradientStop Offset="1" Color="#FF000000" />
                    </LinearGradientBrush>
                </Grid.Background>
            </Grid>
            <Canvas x:Name="MainCanvas" ClipToBounds="True">

                <Image x:Name="ImageControl" RenderTransformOrigin="0.5,0.5" />
                <Canvas
                    x:Name="MaskCanvas"
                    Canvas.Left="0"
                    Canvas.Top="0"
                    IsHitTestVisible="False">
                    <Path Fill="#99000000">
                        <Path.Data>
                            <CombinedGeometry GeometryCombineMode="Exclude">
                                <CombinedGeometry.Geometry1>
                                    <RectangleGeometry Rect="0,0,500,600" />
                                </CombinedGeometry.Geometry1>
                                <CombinedGeometry.Geometry2>
                                    <EllipseGeometry
                                        Center="250,250"
                                        RadiusX="200"
                                        RadiusY="200" />
                                </CombinedGeometry.Geometry2>
                            </CombinedGeometry>
                        </Path.Data>
                    </Path>
                </Canvas>
                <Rectangle
                    x:Name="CropBorder"
                    Canvas.Left="50"
                    Canvas.Top="50"
                    Width="400"
                    Height="400"
                    IsHitTestVisible="False"
                    Stroke="Transparent"
                    StrokeThickness="2" />
            </Canvas>
        </Grid>

    </Grid>
</UserControl>
