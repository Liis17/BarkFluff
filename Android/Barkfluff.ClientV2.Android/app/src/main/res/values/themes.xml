<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Launch/window theme. Цвета и компоненты задаёт Compose (MaterialExpressiveTheme).
         Здесь только NoActionBar + edge-to-edge (через enableEdgeToEdge в MainActivity). -->
    <style name="Theme.Barkfluff" parent="Theme.Material3.DayNight.NoActionBar" />
</resources>
