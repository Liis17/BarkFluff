<?xml version="1.0" encoding="utf-8"?>
<!-- Layout для отправленного сообщения (свои сообщения справа) -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingStart="80dp"
    android:paddingEnd="8dp"
    android:paddingTop="0dp"
    android:paddingBottom="0dp">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:orientation="vertical">

        <!-- Контейнер сообщения -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/messageCard"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:cardBackgroundColor="?attr/colorPrimaryContainer"
            app:cardCornerRadius="20dp"
            app:cardElevation="0dp"
            app:strokeWidth="0dp">

            <LinearLayout
                android:id="@+id/messageContentLayout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:minWidth="80dp">

                <!-- Цитата reply (выше основного текста) -->
                <include
                    android:id="@+id/replyQuote"
                    layout="@layout/view_message_quote" />

                <!-- Контейнер для вложений (картинки edge-to-edge, аудио/видео/файлы с паддингом) -->
                <FrameLayout
                    android:id="@+id/attachmentsContainer"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:visibility="gone"
                    tools:visibility="visible"
                    tools:layout_height="120dp" />

                <!-- Inline upload progress overlay: появляется поверх attachmentsContainer
                     при загрузке медиа (M3 Expressive feedback). Управляется uploadProgress
                     в MessageItem. -->
                <FrameLayout
                    android:id="@+id/uploadProgressOverlay"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:background="@color/scrim_overlay"
                    android:visibility="gone"
                    tools:visibility="gone">

                    <com.google.android.material.progressindicator.CircularProgressIndicator
                        android:id="@+id/uploadProgressBar"
                        android:layout_width="56dp"
                        android:layout_height="56dp"
                        android:layout_gravity="center"
                        android:indeterminate="false"
                        app:indicatorColor="?attr/colorOnPrimary"
                        app:indicatorSize="56dp"
                        app:trackColor="?attr/colorOnPrimary"
                        app:trackThickness="4dp" />

                    <TextView
                        android:id="@+id/uploadProgressLabel"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center"
                        android:textAppearance="?attr/textAppearanceLabelMedium"
                        android:textColor="?attr/colorOnPrimary"
                        tools:text="42%" />

                </FrameLayout>

                <!-- Текст сообщения -->
                <TextView
                    android:id="@+id/messageTextView"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginHorizontal="14dp"
                    android:layout_marginTop="4dp"
                    android:layout_marginBottom="4dp"
                    android:autoLink="web"
                    android:textAppearance="?attr/textAppearanceBodyLarge"
                    android:textColor="?attr/colorOnPrimaryContainer"
                    android:visibility="gone"
                    tools:text="Привет! Как дела?"
                    tools:visibility="visible" />

                <!-- Цитата forward (ниже основного текста) -->
                <include
                    android:id="@+id/forwardQuote"
                    layout="@layout/view_message_quote" />

                <!-- Время и статус (внутри облачка) -->
                <LinearLayout
                    android:id="@+id/timeStatusLayout"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="end"
                    android:layout_marginHorizontal="14dp"
                    android:layout_marginTop="4dp"
                    android:layout_marginBottom="6dp"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <TextView
                        android:id="@+id/editedLabelTextView"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginEnd="6dp"
                        android:text="@string/chat_message_edited"
                        android:textAppearance="?attr/textAppearanceLabelSmall"
                        android:textColor="?attr/colorOnPrimaryContainer"
                        android:alpha="0.7"
                        android:visibility="gone"
                        tools:visibility="visible" />

                    <TextView
                        android:id="@+id/timeTextView"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAppearance="?attr/textAppearanceLabelSmall"
                        android:textColor="?attr/colorOnPrimaryContainer"
                        android:alpha="0.7"
                        tools:text="14:30" />

                    <ImageView
                        android:id="@+id/readStatusImageView"
                        android:layout_width="16dp"
                        android:layout_height="16dp"
                        android:layout_marginStart="4dp"
                        android:contentDescription="@string/cd_read_status"
                        android:visibility="gone"
                        app:tint="?attr/colorOnPrimaryContainer"
                        tools:src="@drawable/ic_double_check"
                        tools:visibility="visible" />

                </LinearLayout>

            </LinearLayout>

        </com.google.android.material.card.MaterialCardView>

        <!-- Стикер (без облачка) -->
        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/stickerImageView"
            android:layout_width="160dp"
            android:layout_height="160dp"
            android:layout_gravity="end"
            android:scaleType="fitCenter"
            android:visibility="gone"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.App.Sticker"
            tools:visibility="gone" />

        <!-- Время и статус для стикера (вне облачка) -->
        <LinearLayout
            android:id="@+id/stickerTimeStatusLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginTop="2dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:visibility="gone">

            <TextView
                android:id="@+id/stickerTimeTextView"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="?attr/textAppearanceLabelSmall"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:alpha="0.7"
                tools:text="14:30" />

            <ImageView
                android:id="@+id/stickerReadStatusImageView"
                android:layout_width="16dp"
                android:layout_height="16dp"
                android:layout_marginStart="4dp"
                android:contentDescription="@string/cd_read_status"
                android:visibility="gone"
                app:tint="?attr/colorOnSurfaceVariant"
                tools:src="@drawable/ic_double_check" />

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
