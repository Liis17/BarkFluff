WEB_SHELL_PORT=7016

# Публичный gRPC-адрес каталога нод. Шелл проксирует в него ListServers,
# больше он никуда не ходит.
NAVIGATOR_SERVICE_HOST=https://navigator.barkfluff.com:443

# Токен Cloudflare Tunnel (Zero Trust -> Networks -> Tunnels -> тот же тунель,
# что и в остальных стеках)
CLOUDFLARE_TUNNEL_TOKEN=...
