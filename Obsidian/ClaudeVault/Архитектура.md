# Архитектура BarkFluff

## Tech Stack

| Слой | Технологии |
|------|-----------|
| **Backend** | .NET 9.0, gRPC (HTTP/2), MassTransit (RabbitMQ), PostgreSQL + EF Core, Redis, Minio (S3), Docker |
| **Windows** | WPF (.NET 10), Code-behind + Reactive wrappers (ReactiveBool/String/Long) |
| **Android** | Kotlin 2.0.0, AGP 8.9.1, gRPC-OkHttp 1.60.0, ViewBinding |
| **Linux** | C++ Qt 6, CMake, gRPC |
| **macOS/iOS** | Swift, SwiftUI, grpc-swift 2.0.0 |

---

## Микросервисы и порты

### Инфраструктура
| Сервис | Порт | Описание |
|--------|------|----------|
| Configuration | 7003 | Централизованная конфигурация и реестр сервисов |
| Beacon | 7002 | Точка входа, предоставляет клиентам адреса сервисов |

### Бизнес-сервисы
| Сервис | Порт | Описание |
|--------|------|----------|
| Identity | 7000 | Auth, JWT, 2FA, сброс пароля, сессии |
| Users | 7001 | Профили, связи, бейджи |
| Messages | 7007 | Сообщения, групповые чаты, read receipts |
| Files | 7005 | Minio, загрузка/скачивание, превью |
| Updates | 7015 | Real-time обновления через gRPC streaming |
| Notification | 7004 | Email-уведомления через SMTP |
| FastAuth | 7008 | QR-авторизация |
| Navigator | 7010 | Регистрация/обнаружение серверов BarkFluff |
| Onliner | 7009 | Трекинг онлайн-статусов |
| Developers | 7020 | Портал документации для разработчиков (gRPC-Web) |

### Дополнительные сервисы
| Сервис | Описание |
|--------|----------|
| AdminPanel | ASP.NET Minimal APIs (порт 51888), HTML+JS+Tailwind дашборд, LiteDB |
| WebServer | REST/HTTP, раздача файлов и статики (порт 64641) |
| Web | gRPC-Web прокси + статика (порт 7016) |
| CloudMessaging | Background Worker, Firebase push-уведомления |
| ClientStorage | Хранилище дистрибутивов клиентов, REST + S3/SQLite |

---

## Service Discovery Flow

1. Каждый сервис при старте запрашивает конфигурацию у `Configuration` через gRPC
2. Межсервисное взаимодействие — прямые gRPC-вызовы
3. Асинхронные события — через RabbitMQ (MassTransit)
4. Beacon собирает адреса всех сервисов и отдаёт клиентам единый `GetServerInfoResponse`

---

## Структура микросервиса

```
BarkFluff.{Service}/
├── Domain/           # Бизнес-сущности
├── Features/         # CQRS-команды (MediatR)
│   └── {Feature}/
│       ├── {Xxx}Command.cs
│       └── {Xxx}CommandHandler.cs
├── Host/             # gRPC-сервис (реализация)
├── Infrastructure/   # Клиенты внешних сервисов
├── Persistence/      # EF Core DbContext, миграции
├── Services/         # Доменные сервисы (JWT, хеширование и т.д.)
├── Settings/         # POCO-конфигурации
├── Program.cs        # Startup
└── Dockerfile
```

---

## Аутентификация — XAuth

Все сервисы используют систему `XAuth` из `BarkFluff.GrpcServer`.

- JWT через заголовок `x-auth-token` (не стандартный Authorization)
- Обязательные metadata-заголовки: `x-device-id`, `x-device-name`, `x-ip`, `x-os`, `x-app-name`, `x-app-version` — все в Base64 (кроме токена)
- Политики авторизации:
  - `TokenType.User` — User или Service токен
  - `TokenType.Service` — только Service токен

```csharp
// Program.cs
builder.Services.AddXAuth(builder.Configuration);
app.UseXAuth();

// Защита gRPC-методов
[Authorize(Policy = nameof(TokenType.User))]
public override Task<XxxResponse> Xxx(XxxRequest request, ServerCallContext context)
```

---

## Startup-конвейер микросервиса

```csharp
builder.LoadConfiguration(ServiceId.Xxx);           // 1. Загрузка конфига из Configuration service
builder.SetRunningAddress(builder.Configuration);   // 2. Настройка Kestrel
builder.AddBarkFluffSerilog("ServiceName");         // 3. Serilog + Seq
builder.Services.AddBarkFluffMetrics("ServiceName"); // 4. Метрики
builder.Services.AddBarkFluffGrpc();                // 5. gRPC + interceptors
builder.Services.AddXAuth(builder.Configuration);   // 6. JWT-аутентификация
app.UseXAuth();                                      // 7. Middleware auth
```

---

## gRPC Client (межсервисные вызовы)

```csharp
builder.Services.AddGrpcClient<UsersServerApi.UsersServerApiClient>(o =>
    {
        o.Address = new Uri(builder.Configuration["UsersService:Host"]);
    })
    .AddInterceptor(() => new JwtClientInterceptor(builder.Configuration["UsersService:Token"]))
    .AddInterceptor(() => new ExceptionClientInterceptor());
```

Токены хранятся в конфигурации под ключами вида `{Service}Service:Token`.

---

## RabbitMQ (MassTransit)

```csharp
builder.Services.AddMassTransit(x =>
{
    x.AddConsumer<SomeConsumer>();
    x.UsingRabbitMq((context, cfg) =>
    {
        cfg.Host(builder.Configuration["RabbitMQ:Host"], "/", h =>
        {
            h.Username(builder.Configuration["RabbitMQ:Username"]);
            h.Password(builder.Configuration["RabbitMQ:Password"]);
        });
        cfg.ReceiveEndpoint("queue-name", e =>
        {
            e.ConfigureConsumer<SomeConsumer>(context);
        });
    });
});
```

Типы событий — в [[Shared/Queue]].

---

## Proto Files

Все proto-определения в `Shared/BarkFluff.Proto/`. Детали → [[Shared/Proto]].

```xml
<!-- Серверная сторона -->
<Protobuf Include="..\..\Shared\BarkFluff.Proto\xxx_api.proto" GrpcServices="Server" />

<!-- Клиентская сторона -->
<Protobuf Include="..\..\Shared\BarkFluff.Proto\xxx_api.proto" GrpcServices="Client" />

<!-- Только типы (без сервисного кода) -->
<Protobuf Include="..\..\Shared\BarkFluff.Proto\shared.proto" GrpcServices="None" />
```

---

## CQRS-паттерн (MediatR)

```csharp
// Команда
public class XxxCommand : IRequest<XxxResponse>
{
    // свойства из gRPC запроса
}

// Хендлер
public class XxxCommandHandler : IRequestHandler<XxxCommand, XxxResponse>
{
    public async Task<XxxResponse> Handle(XxxCommand request, CancellationToken ct) { }
}

// gRPC-метод
[Authorize(Policy = nameof(TokenType.User))]
public override async Task<XxxResponse> Xxx(XxxRequest request, ServerCallContext context)
{
    return await _mediator.Send(new XxxCommand { /* map */ });
}
```

MediatR автоматически регистрирует хендлеры из сборки.

---

## Добавление нового сервиса

1. Добавить `ServiceId` в `Shared/BarkFluff.Shared.Identity/ServiceId.cs`
2. Зарегистрировать в БД Configuration service
3. Proto-файлы добавить в `Shared/BarkFluff.Proto/`
4. Использовать `builder.LoadConfiguration(ServiceId.NewService)` в Program.cs

---

## Shared Libraries

| Библиотека | Путь | Назначение |
|-----------|------|-----------|
| BarkFluff.Proto | `Shared/BarkFluff.Proto/` | Все .proto файлы |
| BarkFluff.Shared.Auth | `Shared/BarkFluff.Shared.Auth/` | gRPC client interceptors |
| BarkFluff.Shared.Identity | `Shared/BarkFluff.Shared.Identity/` | ServiceId, TokenType, IdentityClaims |
| BarkFluff.Shared.Exceptions | `Shared/BarkFluff.Shared.Exceptions/` | BaseGrpcException, ExceptionClientInterceptor |
| BarkFluff.Shared.Queue | `Shared/BarkFluff.Shared.Queue/` | RabbitMQ события |
| BarkFluff.Shared.SecurityUtilities | `Shared/BarkFluff.Shared.SecurityUtilities/` | Утилиты безопасности |
| BarkFluff.GrpcServer | `Backend/BarkFluff.GrpcServer/` | XAuth, interceptors, Serilog, Metrics |

---

## CI/CD — GitHub Actions

### Структура пайплайна (с 2025)

Вместо единого `build.yml` (~5000 строк) — **отдельный workflow-файл на каждый микросервис**.

| Папка/файл | Назначение |
|------------|-----------|
| `.github/workflows/build-backend-{service}.yml` | По одному файлу на каждый из 17 backend-сервисов |
| `.github/workflows/build-client-macos.yml` | Сборка macOS-клиента (GitHub runner `macos-latest`) |
| `.github/workflows/build-client-wpf.yml` | Сборка WPF-клиента (self-hosted runner) |
| `.github/workflows/build-client-android.yml` | Сборка Android APK (self-hosted runner) |
| `.github/workflows/build.yml.old` | Архив старого монолитного workflow |

### Подход к сборке backend-микросервисов (self-hosted Linux runner)

**Трёхшаговая модель** для облегчения Docker-образов:

1. **`check-dotnet`** — проверяет наличие нужного .NET SDK (9.0 или 10.0) на раннере. При отсутствии — отправляет ошибку в Telegram и завершает пайплайн.
2. **`request-approval`** — запрашивает подтверждение через Telegram-бот (`powerdot/telegram-manual-approval`).
3. **`build`** — `dotnet publish` на раннере (Release, Linux) → `docker build` из `Dockerfile.slim` → `docker push` в приватный registry.

### Dockerfile.slim

Каждый сервис имеет `Dockerfile.slim` — облегчённый образ **без слоя SDK**:
- Только `FROM aspnet:X.Y-{variant}` (runtime)
- `COPY publish/ .` — копирует pre-built артефакты с раннера
- Для сервисов с chiseled-образами: `COPY --chown=1654:1654`, директории (`logs/`, `data/`) создаются на раннере перед Docker-сборкой
- `BarkFluff.Web` — особый случай: Node/protoc-генерация остаётся в Docker (stage `proto-build`), .NET собирается на раннере

### .NET версии по сервисам

| Версия | Сервисы |
|--------|---------|
| **.NET 9.0** | Identity, Users, Messages, Files, Updates, Notification, Configuration, Beacon, FastAuth, Navigator, CloudMessaging, ClientStorage, Web, Developers |
| **.NET 10.0** | Onliner, AdminPanel, WebServer |
| **.NET 10.0** | WPF Client (`net10.0-windows`) |

### Telegram-уведомления

- ✅ Запрос подтверждения ("Собрать?") через бот
- ✅ Ошибка на любом этапе (check-dotnet / docker build)
- ✅ Успешная сборка
- ❌ Уведомление об отмене сборки **не отправляется**
