<UserControl x:Class="BarkFluff.ClientV2.WPF.Views.ConnectedNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <UserControl.RenderTransform>
        <TranslateTransform Y="12" />
    </UserControl.RenderTransform>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.22" />
                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.(TranslateTransform.Y)" From="12" To="0" Duration="0:0:0.22" />
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>
    <Grid Margin="40,24,40,40">
        <ui:Card Width="580"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 Style="{StaticResource OnboardingCard}">
            <StackPanel>
                <Border Width="52"
                        Height="52"
                        HorizontalAlignment="Left"
                        Background="{DynamicResource OnboardingAccentSurfaceBrush}"
                        CornerRadius="16">
                    <TextBlock HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               FontSize="24"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource SystemAccentColorPrimaryBrush}"
                               Text="✓" />
                </Border>
                <TextBlock Margin="0,22,0,0"
                           Style="{StaticResource OnboardingTitle}"
                           Text="{DynamicResource ConnectedNode_Title}" />
                <TextBlock Margin="0,10,0,0"
                           Style="{StaticResource OnboardingDescription}"
                           Text="{DynamicResource ConnectedNode_Subtitle}" />
                <Border Margin="0,24,0,0"
                        Padding="18"
                        Background="{DynamicResource OnboardingSubtleSurfaceBrush}"
                        CornerRadius="12">
                    <StackPanel>
                        <Border HorizontalAlignment="Left" Style="{StaticResource StatusPill}">
                            <TextBlock FontSize="12"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource SystemAccentColorPrimaryBrush}"
                                       Text="{DynamicResource ConnectedNode_Status}" />
                        </Border>
                        <TextBlock Margin="0,14,0,0"
                                   FontSize="20"
                                   FontWeight="SemiBold"
                                   Text="{Binding Node.Name}" />
                        <TextBlock Margin="0,6,0,0"
                                   Style="{StaticResource MutedText}"
                                   Text="{Binding Node.Description}"
                                   TextWrapping="Wrap" />
                        <TextBlock Margin="0,10,0,0"
                                   FontFamily="Cascadia Mono"
                                   FontSize="12"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   Text="{Binding Node.BeaconAddress}" />
                    </StackPanel>
                </Border>
                <ui:Button Margin="0,24,0,0"
                           HorizontalAlignment="Left"
                           Command="{Binding ChooseAnotherNodeCommand}"
                           Content="{DynamicResource ConnectedNode_ChooseAnother}" />
            </StackPanel>
        </ui:Card>
    </Grid>
</UserControl>
