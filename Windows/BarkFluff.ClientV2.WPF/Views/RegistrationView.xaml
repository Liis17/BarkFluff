<UserControl x:Class="BarkFluff.ClientV2.WPF.Views.RegistrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">
    <Grid Margin="40,24,40,40">
        <ui:Card Width="560" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{StaticResource OnboardingCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Visibility="{Binding IsProfileStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Style="{StaticResource OnboardingTitle}" Text="{DynamicResource Registration_Title}" />
                    <TextBlock Margin="0,10,0,24" Style="{StaticResource OnboardingDescription}" Text="{DynamicResource Registration_Subtitle}" />
                    <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource Registration_FirstName}" />
                    <ui:TextBox Margin="0,8,0,14" Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource Registration_LastName}" />
                    <ui:TextBox Margin="0,8,0,14" Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource Registration_Username}" />
                    <ui:TextBox Margin="0,8,0,14" PlaceholderText="{DynamicResource Registration_UsernameHint}" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource Registration_Email}" />
                    <ui:TextBox Margin="0,8,0,20" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
                    <ui:Button HorizontalAlignment="Left" Appearance="Primary" Command="{Binding CreateAccountCommand}" Content="{DynamicResource Registration_SendCode}" />
                </StackPanel>

                <StackPanel Grid.Row="0" Visibility="{Binding IsConfirmationStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Style="{StaticResource OnboardingTitle}" Text="{DynamicResource Registration_ConfirmTitle}" />
                    <TextBlock Margin="0,10,0,24" Style="{StaticResource OnboardingDescription}" Text="{DynamicResource Registration_ConfirmSubtitle}" />
                    <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource Registration_ConfirmationCode}" />
                    <ui:TextBox Margin="0,8,0,20" MaxLength="6" Text="{Binding ConfirmationCode, UpdateSourceTrigger=PropertyChanged}" />
                    <ui:Button HorizontalAlignment="Left" Appearance="Primary" Command="{Binding ConfirmAccountCommand}" Content="{DynamicResource Registration_Confirm}" />
                </StackPanel>

                <StackPanel Grid.Row="0" Visibility="{Binding IsPasswordStep, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <TextBlock Style="{StaticResource OnboardingTitle}" Text="{DynamicResource Registration_PasswordTitle}" />
                    <TextBlock Margin="0,10,0,24" Style="{StaticResource OnboardingDescription}" Text="{DynamicResource Registration_PasswordSubtitle}" />
                    <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource Registration_Password}" />
                    <ui:PasswordBox Margin="0,8,0,14" Password="{Binding Password, UpdateSourceTrigger=PropertyChanged}" RevealButtonEnabled="True" />
                    <TextBlock Style="{StaticResource SectionTitle}" Text="{DynamicResource Registration_PasswordConfirmation}" />
                    <ui:PasswordBox Margin="0,8,0,20" Password="{Binding PasswordConfirmation, UpdateSourceTrigger=PropertyChanged}" RevealButtonEnabled="True" />
                    <ui:Button HorizontalAlignment="Left" Appearance="Primary" Command="{Binding SetPasswordCommand}" Content="{DynamicResource Registration_Finish}" />
                </StackPanel>

                <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
                    <TextBlock Margin="0,18,0,0" Foreground="{DynamicResource SystemFillColorCriticalBrush}" Text="{Binding ErrorMessage}" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" />
                    <TextBlock Margin="0,8,0,0" Foreground="{DynamicResource SystemAccentColorPrimaryBrush}" Text="{Binding StatusMessage}" Visibility="{Binding StatusMessage, Converter={StaticResource StringToVisibilityConverter}}" />
                    <StackPanel Margin="0,16,0,0" Orientation="Horizontal">
                        <ui:HyperlinkButton Command="{Binding ShowLoginCommand}" Content="{DynamicResource Common_BackToLogin}" />
                        <ui:ProgressRing Width="22" Height="22" Margin="14,0,0,0" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
                    </StackPanel>
                </StackPanel>
            </Grid>
        </ui:Card>
    </Grid>
</UserControl>
