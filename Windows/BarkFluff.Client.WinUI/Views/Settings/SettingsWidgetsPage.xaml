<Page
    x:Class="BarkFluff.Client.WinUI.Views.Settings.SettingsWidgetsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <StackPanel Margin="32,24,32,40"
                    MaxWidth="720"
                    HorizontalAlignment="Left">
            <TextBlock Margin="0,0,0,20"
                       Style="{StaticResource OnboardingTitle}"
                       Text="{StaticResource Settings_Widgets_Title}" />

            <!--
                Аналога виджетов рабочего стола Android в Windows нет: это отдельный пакет
                widget-provider с адаптивными карточками, а не экран внутри приложения.
            -->
            <Border Style="{StaticResource SettingsGroupCard}">
                <StackPanel Margin="16,20">
                    <TextBlock Text="{StaticResource Settings_Widgets_Empty}" TextWrapping="Wrap" />
                    <TextBlock Margin="0,8,0,0"
                               Style="{StaticResource MutedText}"
                               Text="{StaticResource Settings_Widgets_AndroidHint}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
