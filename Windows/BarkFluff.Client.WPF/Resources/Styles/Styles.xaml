<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:extensions="clr-namespace:BarkFluff.Client.WPF.UserControls.Extensions"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <!--  Текст на кнопке  -->
    <Style x:Key="MainButtonTextLeft" TargetType="ui:TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="35,0,0,0" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="Foreground" Value="White" />
    </Style>
    <Style x:Key="MainButtonTextRight" TargetType="ui:TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,0,35,0" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="Foreground" Value="White" />
    </Style>
    <Style x:Key="MainButtonTextSingle" TargetType="ui:TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="35,0,35,0" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="Foreground" Value="White" />
    </Style>
    <Style x:Key="ButtonSymbolRight" TargetType="ui:SymbolIcon">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="10,0,35,0" />
    </Style>
    <Style x:Key="ButtonSymbolLeft" TargetType="ui:SymbolIcon">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="35,0,10,0" />
    </Style>
    <Style x:Key="ButtonSymbolSingle" TargetType="ui:SymbolIcon">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="35,0,35,0" />
    </Style>

    <!--  Главный стиль кнопки  -->
    <Style x:Key="MainButton" TargetType="{x:Type ui:Button}">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinWidth" Value="50" />
        <Setter Property="Padding" Value="55,112" />
        <Setter Property="Height" Value="45" />
        <Setter Property="Margin" Value="0,0,0,20" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Background" Value="#df5000" />
        <Setter Property="CornerRadius" Value="22" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="MouseOverBackground" Value="#FFC54700" />
        <Setter Property="PressedBackground" Value="#FFAB3D00" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:Button}">
                    <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderThickness="1"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>




    <Style x:Key="MinimalTextBox" TargetType="ui:TextBox">
        <Setter Property="Background" Value="#282828" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontFamily" Value="AdwaitaSans" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Padding" Value="15,12" />
        <Setter Property="BorderBrush" Value="#404040" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="White" />
        <Setter Property="PlaceholderEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ui:TextBox">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="8">
                        <Grid>
                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Margin="{TemplateBinding Padding}"
                                Focusable="False"
                                HorizontalScrollBarVisibility="Hidden"
                                VerticalScrollBarVisibility="Hidden" />
                            <TextBlock
                                x:Name="PlaceholderTextBlock"
                                Margin="{TemplateBinding Padding}"
                                VerticalAlignment="Center"
                                Foreground="#FF9E9E9E"
                                IsHitTestVisible="False"
                                Text="{TemplateBinding PlaceholderText}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="" />
                                                    <Condition Binding="{Binding PlaceholderEnabled, RelativeSource={RelativeSource TemplatedParent}}" Value="True" />
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Visibility" Value="Visible" />
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="#6D90F3" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="#505050" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Стиль для текстового поля ввода сообщения (прозрачный фон, без рамки)  -->
    <Style
        x:Key="MessageInputTextBox"
        BasedOn="{StaticResource MinimalTextBox}"
        TargetType="ui:TextBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="-5,10" />
        <Setter Property="Foreground" Value="White" />
    </Style>

    <Style x:Key="InfoTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="AdwaitaSans" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Foreground" Value="#B0B0B0" />
        <Setter Property="Margin" Value="0,2,0,0" />
    </Style>

    <Style x:Key="MinimalPasswordBox" TargetType="PasswordBox">
        <Setter Property="Background" Value="#282828" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontFamily" Value="AdwaitaSans" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Padding" Value="15,12" />
        <Setter Property="BorderBrush" Value="#404040" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="8">
                        <ScrollViewer x:Name="PART_ContentHost" Padding="{TemplateBinding Padding}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="#6D90F3" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="#505050" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="VerificationTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="#282828" />
        <Setter Property="Width" Value="45" />
        <Setter Property="Margin" Value="4" />
        <Setter Property="MaxLength" Value="1" />
        <Setter Property="Height" Value="45" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontFamily" Value="AdwaitaSans" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="BorderBrush" Value="#404040" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="White" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="8">
                        <ScrollViewer x:Name="PART_ContentHost" Padding="8" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="BorderBrush" Value="#6D90F3" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="#505050" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <!--  Windows 11 style card container  -->
    <Style x:Key="Win11CardStyle" TargetType="Border">
        <Setter Property="Background" Value="#2D2D2D" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Padding" Value="24" />
        <Setter Property="BorderBrush" Value="#3D3D3D" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect
                    BlurRadius="20"
                    Direction="270"
                    Opacity="0.3"
                    ShadowDepth="6"
                    Color="Black" />
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Windows 11 style accent button for PIN code pages  -->
    <Style x:Key="Win11AccentButton" TargetType="{x:Type ui:Button}">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinWidth" Value="200" />
        <Setter Property="Height" Value="48" />
        <Setter Property="Margin" Value="0,24,0,0" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Background" Value="#df5000" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="MouseOverBackground" Value="#FF6B1A" />
        <Setter Property="PressedBackground" Value="#C44700" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:Button}">
                    <Grid>
                        <Border
                            x:Name="ShadowBorder"
                            Background="Transparent"
                            CornerRadius="{TemplateBinding CornerRadius}">
                            <Border.Effect>
                                <DropShadowEffect
                                    BlurRadius="12"
                                    Direction="270"
                                    Opacity="0.4"
                                    ShadowDepth="3"
                                    Color="#df5000" />
                            </Border.Effect>
                        </Border>
                        <Border
                            x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Setter TargetName="ShadowBorder" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="16"
                                        Direction="270"
                                        Opacity="0.5"
                                        ShadowDepth="4"
                                        Color="#df5000" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Setter TargetName="ShadowBorder" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="8"
                                        Direction="270"
                                        Opacity="0.3"
                                        ShadowDepth="2"
                                        Color="#df5000" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>

    <!--  Windows 11 style accent button for PIN code pages  -->
    <Style x:Key="Win11Default" TargetType="{x:Type ui:Button}">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinWidth" Value="100" />
        <Setter Property="Height" Value="35" />
        <Setter Property="Margin" Value="0,0,0,0" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Background" Value="#2d2d2d" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="MouseOverBackground" Value="#2f2f2f" />
        <Setter Property="PressedBackground" Value="#3d3d3d" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:Button}">
                    <Grid>
                        <Border
                            x:Name="ShadowBorder"
                            Background="Transparent"
                            CornerRadius="{TemplateBinding CornerRadius}">
                            <Border.Effect>
                                <DropShadowEffect
                                    BlurRadius="12"
                                    Direction="270"
                                    Opacity="0.4"
                                    ShadowDepth="3"
                                    Color="#2d2d2d" />
                            </Border.Effect>
                        </Border>
                        <Border
                            x:Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="{TemplateBinding CornerRadius}"
                            SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Setter TargetName="ShadowBorder" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="16"
                                        Direction="270"
                                        Opacity="0.5"
                                        ShadowDepth="4"
                                        Color="#1f1f1f" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                            <Setter TargetName="ShadowBorder" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect
                                        BlurRadius="8"
                                        Direction="270"
                                        Opacity="0.3"
                                        ShadowDepth="2"
                                        Color="#2d2d2d" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>

    <!--  Windows 11 style danger button  -->
    <Style x:Key="Win11DangerButton" TargetType="{x:Type ui:Button}">
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="MinWidth" Value="180" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Margin" Value="0,8,0,0" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Background" Value="#D42020" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="MouseOverBackground" Value="#E63030" />
        <Setter Property="PressedBackground" Value="#B01818" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:Button}">
                    <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderThickness="0"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>

    <Style x:Key="LinkTextBlock" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#6D90F3" />
        <Setter Property="FontFamily" Value="AdwaitaSans" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="TextDecorations" Value="None" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="#5A7AE6" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="AdwaitaSans" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,0,0,30" />
    </Style>

    <Style x:Key="FieldLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="AdwaitaSans" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="Foreground" Value="#E0E0E0" />
        <Setter Property="Margin" Value="0,0,0,8" />
    </Style>


    <!--  текстовое поле ввода у чата  -->

    <Style x:Key="TextField" TargetType="ui:TextBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ui:TextBox">
                    <Grid>
                        <Border
                            x:Name="ContentBorder"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="15">
                            <Grid Margin="{TemplateBinding Padding}">
                                <ui:PassiveScrollViewer x:Name="PART_ContentHost" Foreground="{TemplateBinding Foreground}" />
                                <TextBlock
                                    x:Name="Placeholder"
                                    Foreground="{DynamicResource TextBoxPlaceholderForegroundBrush}"
                                    Text="{TemplateBinding PlaceholderText}"
                                    Visibility="Collapsed" />
                            </Grid>
                        </Border>
                        <Border
                            x:Name="AccentBorder"
                            BorderBrush="Transparent"
                            BorderThickness="0"
                            CornerRadius="15" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="AccentBorder" Property="BorderBrush" Value="Red" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="AccentBorder" Property="BorderThickness" Value="0" />
                            <Setter TargetName="AccentBorder" Property="BorderBrush" Value="Bisque" />
                        </Trigger>
                        <Trigger Property="PlaceholderEnabled" Value="True">
                            <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>



    <!--  новые стили для страницы профиля  -->

    <!--  Цвета для темы  -->
    <SolidColorBrush x:Key="SystemAccentColorSecondaryBrush" Color="#FF4C6EF5" />
    <SolidColorBrush x:Key="SystemAccentColorLightBrush" Color="#E3F2FD" />
    <SolidColorBrush x:Key="SystemAccentColorDarkBrush" Color="#1976D2" />

    <!--  Фоны карточек  -->
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFFFF" />

    <!--  Темная тема  -->
    <SolidColorBrush x:Key="CardBackgroundBrushDark" Color="#FF2D2D30" />

    <!--  Текстовые кисти  -->
    <SolidColorBrush x:Key="TextFillColorPrimaryBrush" Color="#FF000000" />
    <SolidColorBrush x:Key="TextFillColorSecondaryBrush" Color="#FF666666" />

    <!--  Темная тема для текста  -->
    <SolidColorBrush x:Key="TextFillColorPrimaryBrushDark" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="TextFillColorSecondaryBrushDark" Color="#FFAAAAAA" />

    <!--  Контрольные кисти  -->
    <SolidColorBrush x:Key="ControlStrokeColorDefaultBrush" Color="#FFE5E5E5" />
    <SolidColorBrush x:Key="ControlStrokeColorDefaultBrushDark" Color="#FF404040" />

    <!--  Динамические ресурсы с поддержкой темной темы  -->
    <Style x:Key="DynamicCardBackground" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Source={x:Static SystemParameters.GradientCaptions}}" Value="True">
                <Setter Property="Background" Value="{StaticResource CardBackgroundBrushDark}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DynamicTextPrimary" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextFillColorPrimaryBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}" Value="True">
                <Setter Property="Foreground" Value="{StaticResource TextFillColorPrimaryBrushDark}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="DynamicTextSecondary" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextFillColorSecondaryBrush}" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Source={x:Static SystemParameters.HighContrast}}" Value="True">
                <Setter Property="Foreground" Value="{StaticResource TextFillColorSecondaryBrushDark}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--  Градиент для профиля  -->
    <LinearGradientBrush x:Key="ProfileHeaderGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Offset="0" Color="#FF6B73FF" />
        <GradientStop Offset="1" Color="#FF9854FF" />
    </LinearGradientBrush>

    <!--  Альтернативные градиенты  -->
    <LinearGradientBrush x:Key="ProfileHeaderGradientBlue" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Offset="0" Color="#FF667EEA" />
        <GradientStop Offset="1" Color="#FF764BA2" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ProfileHeaderGradientGreen" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Offset="0" Color="#FF11998E" />
        <GradientStop Offset="1" Color="#FF38EF7D" />
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ProfileHeaderGradientOrange" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Offset="0" Color="#FFFA709A" />
        <GradientStop Offset="1" Color="#FFFEE140" />
    </LinearGradientBrush>

    <!--  Стиль для баджей  -->
    <Style x:Key="BadgeStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SystemAccentColorSecondaryBrush}" />
        <Setter Property="CornerRadius" Value="12" />
        <Setter Property="Width" Value="24" />
        <Setter Property="Height" Value="24" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
    </Style>

    <!--  Стили для разных типов баджей  -->
    <Style
        x:Key="VipBadgeStyle"
        BasedOn="{StaticResource BadgeStyle}"
        TargetType="Border">
        <Setter Property="Background" Value="Gold" />
    </Style>

    <Style
        x:Key="VerifiedBadgeStyle"
        BasedOn="{StaticResource BadgeStyle}"
        TargetType="Border">
        <Setter Property="Background" Value="LightBlue" />
    </Style>

    <Style
        x:Key="PopularBadgeStyle"
        BasedOn="{StaticResource BadgeStyle}"
        TargetType="Border">
        <Setter Property="Background" Value="Orange" />
    </Style>

    <!--  Стиль для информационных карточек  -->
    <Style x:Key="InfoCardStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="16,12" />
        <Setter Property="Margin" Value="0,4" />
        <Setter Property="BorderBrush" Value="{StaticResource ControlStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Style.Triggers>
            <!--  Hover эффект  -->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource SystemAccentColorLightBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  Стиль для активной вкладки  -->
    <Style x:Key="ActiveTabButtonStyle" TargetType="ui:Button">
        <Setter Property="Background" Value="{StaticResource SystemAccentColorLightBrush}" />
        <Setter Property="Foreground" Value="{StaticResource SystemAccentColorDarkBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource SystemAccentColorSecondaryBrush}" />
        <Setter Property="BorderThickness" Value="2" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <!--  Стиль для неактивной вкладки  -->
    <Style x:Key="InactiveTabButtonStyle" TargetType="ui:Button">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource TextFillColorSecondaryBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource ControlStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="6" />
        <Setter Property="Padding" Value="12,8" />
        <Setter Property="FontWeight" Value="Normal" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource SystemAccentColorLightBrush}" />
                <Setter Property="Foreground" Value="{StaticResource SystemAccentColorDarkBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <!--  Статус индикаторы  -->
    <SolidColorBrush x:Key="OnlineStatusBrush" Color="#4CAF50" />
    <SolidColorBrush x:Key="RecentStatusBrush" Color="#FFC107" />
    <SolidColorBrush x:Key="OfflineStatusBrush" Color="#9E9E9E" />

    <!--  Placeholder для аватара по умолчанию  -->
    <DrawingImage x:Key="DefaultAvatarImage">
        <DrawingImage.Drawing>
            <DrawingGroup ClipGeometry="M0,0 V100 H100 V0 H0 Z">
                <GeometryDrawing Brush="#FFE0E0E0" Geometry="F1 M100,100z M0,0z M0,0L100,0L100,100L0,100z" />
                <GeometryDrawing Brush="#FF9E9E9E">
                    <GeometryDrawing.Geometry>
                        <EllipseGeometry
                            Center="50,35"
                            RadiusX="15"
                            RadiusY="15" />
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
                <GeometryDrawing Brush="#FF9E9E9E">
                    <GeometryDrawing.Geometry>
                        <PathGeometry Figures="M25,70 Q50,55 75,70 L75,100 L25,100 Z" />
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingImage.Drawing>
    </DrawingImage>

    <!--  SymbolIcon для настроек  -->
    <Style x:Key="SettingsSymbolIcon" TargetType="ui:SymbolIcon">
        <Setter Property="Width" Value="40" />
        <Setter Property="Height" Value="40" />
        <Setter Property="Margin" Value="10,10,5,10" />
        <Setter Property="FontSize" Value="40" />
        <Setter Property="Filled" Value="False" />
        <Setter Property="Foreground" Value="#FFEDEDED" />
    </Style>

    <!--  Стиль текста для кнопок на странице настроек  -->
    <Style x:Key="SettingsMenuBlockText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="AdwaitaSans" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="#FFE4E4E4" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Margin" Value="0,0,0,0" />
    </Style>

    <!--  Стиль текста для окна после уведомления  -->
    <Style x:Key="PostUpdateStyleText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="AdwaitaSans" />
        <Setter Property="FontSize" Value="26" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Foreground" Value="#FFE6E6E6" />
    </Style>


    <!--  Footer separator style  -->
    <Style x:Key="FooterSeparatorStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="Light" />
        <Setter Property="Foreground" Value="#FFF39E6D" />
        <Setter Property="Opacity" Value="0.5" />
        <Setter Property="Margin" Value="12,0" />
    </Style>




    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->
    <!--  //////////////////////////////////////////////////////////////////////  -->

    <!--  новые стили  -->

    <Style x:Key="ButtonStyle" TargetType="{x:Type ui:Button}">
        <Setter Property="Cursor" Value="Hand" />

        <Setter Property="Height" Value="48" />
        <Setter Property="Padding" Value="24,0" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="UseLayoutRounding" Value="True" />
    </Style>
    <Style x:Key="ButtonTextStyle" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Margin" Value="5,0,5,0" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="16" />
    </Style>
    <Style x:Key="ButtonIconStyle" TargetType="{x:Type ui:SymbolIcon}">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Margin" Value="5,0,5,0" />
        <Setter Property="Filled" Value="True" />
    </Style>

    <!--  Адаптивный цвет кнопки под цвет приложения  -->
    <Style
        x:Key="AdaptiveButton"
        BasedOn="{StaticResource ButtonStyle}"
        TargetType="{x:Type ui:Button}">
        <Setter Property="Background" Value="{DynamicResource ButtonBackground}" />
        <Setter Property="MouseOverBackground" Value="{DynamicResource ButtonMouseOverBackground}" />
        <Setter Property="PressedBackground" Value="{DynamicResource ButtonPressedBackground}" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect
                    BlurRadius="16"
                    Direction="270"
                    Opacity="0.3"
                    ShadowDepth="4"
                    Color="{DynamicResource ButtonDropShadow}" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:Button}">
                    <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderThickness="0"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            Margin="15,0,15,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <Style
        x:Key="AdaptiveButtonTextStyle"
        BasedOn="{StaticResource ButtonTextStyle}"
        TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource ButtonText}" />
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />

    </Style>
    <Style
        x:Key="AdaptiveButtonIconStyle"
        BasedOn="{StaticResource ButtonIconStyle}"
        TargetType="{x:Type ui:SymbolIcon}">
        <Setter Property="Foreground" Value="{DynamicResource ButtonText}" />
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
    </Style>

    <!--  Цвет кнопки для обязательного действия  -->
    <Style
        x:Key="RequiredButton"
        BasedOn="{StaticResource ButtonStyle}"
        TargetType="{x:Type ui:Button}">
        <Setter Property="Background" Value="{DynamicResource RequiredButtonBackground}" />
        <Setter Property="MouseOverBackground" Value="{DynamicResource RequiredButtonMouseOverBackground}" />
        <Setter Property="PressedBackground" Value="{DynamicResource RequiredButtonPressedBackground}" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect
                    BlurRadius="16"
                    Direction="270"
                    Opacity="0.3"
                    ShadowDepth="4"
                    Color="{DynamicResource RequiredButtonDropShadow}" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:Button}">
                    <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderThickness="0"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="True">
                        <ContentPresenter
                            Margin="15,0,15,0"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding MouseOverBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{Binding PressedBackground, RelativeSource={RelativeSource TemplatedParent}}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

    </Style>
    <Style
        x:Key="RequiredButtonTextStyle"
        BasedOn="{StaticResource ButtonTextStyle}"
        TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource RequiredButtonText}" />
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
    </Style>
    <Style
        x:Key="RequiredButtonIconStyle"
        BasedOn="{StaticResource ButtonIconStyle}"
        TargetType="{x:Type ui:SymbolIcon}">
        <Setter Property="Foreground" Value="{DynamicResource RequiredButtonText}" />
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
    </Style>

    <!--  Стиль текста описания  -->
    <Style x:Key="DescriptionTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="14" />
        <Setter Property="Foreground" Value="{DynamicResource DescriptionText}" />
        <Setter Property="Opacity" Value="1" />
    </Style>

    <!--  Стиль текста подписей  -->
    <Style x:Key="SubtitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Medium" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="18" />
        <Setter Property="Foreground" Value="{DynamicResource SubtitleText}" />
        <Setter Property="Opacity" Value="1" />
    </Style>

    <!--  Стиль текста заголовков  -->
    <Style x:Key="BigTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="FontSize" Value="42" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="42" />
        <Setter Property="Foreground" Value="{DynamicResource TitleText}" />
        <Setter Property="Opacity" Value="1" />
    </Style>
    <Style x:Key="TitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="FontSize" Value="22" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="22" />
        <Setter Property="Foreground" Value="{DynamicResource TitleText}" />
        <Setter Property="Opacity" Value="1" />
    </Style>
    <Style x:Key="LittleTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="UltraBold" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="14" />
        <Setter Property="Foreground" Value="{DynamicResource TitleText}" />
        <Setter Property="Opacity" Value="1" />
    </Style>

    <!--  Стиль нижнего колонтитула для ссылок  -->
    <Style x:Key="FooterLinkStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Foreground" Value="#FFF39E6D" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Opacity" Value="0.85" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="#FFFFB088" />
                <Setter Property="Opacity" Value="1" />
            </Trigger>
        </Style.Triggers>
    </Style>
    <!--  Стиль подсказок  -->
    <Style x:Key="TipsStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontWeight" Value="Light" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="LineHeight" Value="12" />
        <Setter Property="Foreground" Value="{DynamicResource TipsColor}" />
        <Setter Property="Opacity" Value="1" />
    </Style>

    <!--  Бордеры  -->

    <Style x:Key="MainStyleBorder" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource BorderBlock}" />
        <Setter Property="CornerRadius" Value="16" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Padding" Value="30" />
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect
                    BlurRadius="40"
                    Direction="270"
                    Opacity="0.25"
                    ShadowDepth="8"
                    Color="Black" />
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="PinCodeTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource TextBackground}" />
        <Setter Property="Width" Value="64" />
        <Setter Property="Height" Value="64" />
        <Setter Property="Margin" Value="8" />
        <Setter Property="MaxLength" Value="1" />
        <Setter Property="Foreground" Value="{DynamicResource TextForegraund}" />
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="FontSize" Value="26" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="BorderBrush" Value="{DynamicResource TextBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="{DynamicResource TextCaretBrush}" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Grid>
                        <!--  Shadow effect layer  -->
                        <Border
                            x:Name="ShadowBorder"
                            Background="Transparent"
                            CornerRadius="14" />
                        <!--  Main content border  -->
                        <Border
                            x:Name="ContentBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="14">
                            <Grid>
                                <ScrollViewer
                                    x:Name="PART_ContentHost"
                                    Margin="0"
                                    Padding="0"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <!--  Focus glow effect  -->
                        <Border
                            x:Name="FocusGlow"
                            Background="Transparent"
                            BorderBrush="{DynamicResource TextGlowBrush}"
                            BorderThickness="2"
                            CornerRadius="14"
                            Opacity="0" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource TextGlowBrush}" />
                            <Setter TargetName="ContentBorder" Property="BorderThickness" Value="2" />
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource TextBackground}" />
                            <Setter TargetName="FocusGlow" Property="Opacity" Value="0.4" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ContentBorder" Property="BorderBrush" Value="{DynamicResource TextBorderBrush}" />
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource TextBackground}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsFocused" Value="False" />
                                <Condition Property="Text" Value="" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentBorder" Property="Background" Value="{DynamicResource TextBackground}" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="InputTextBoxStyle" TargetType="{x:Type ui:TextBox}">
        <!--  Основные цвета (можно вынести в ресурсы, но здесь для наглядности)  -->
        <Setter Property="Background" Value="{DynamicResource TextBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource TextForegraund}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TextBorderBrush}" />
        <Setter Property="CaretBrush" Value="{DynamicResource TextCaretBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Height" Value="56" />

        <!--  Шрифты  -->
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontWeight" Value="Normal" />

        <!--  Выравнивание  -->
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Padding" Value="8,8" />
        <Setter Property="Margin" Value="4" />

        <!--  Включаем возможность скролла, если текста много  -->
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Hidden" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:TextBox}">
                    <Border
                        x:Name="BorderElement"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="8">

                        <Grid>
                            <!--  Плейсхолдер (Текст-подсказка)  -->
                            <TextBlock
                                x:Name="PlaceholderTextElement"
                                Margin="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                FontFamily="{TemplateBinding FontFamily}"
                                FontSize="{TemplateBinding FontSize}"
                                Foreground="{DynamicResource TextPlaceholder}"
                                IsHitTestVisible="False"
                                Text="{TemplateBinding PlaceholderText}"
                                Visibility="Collapsed" />

                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Margin="0"
                                Padding="{TemplateBinding Padding}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                IsTabStop="False" />
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <!--  Логика показа Плейсхолдера: Если текста нет - показываем  -->
                        <Trigger Property="Text" Value="{x:Null}">
                            <Setter TargetName="PlaceholderTextElement" Property="Visibility" Value="Visible" />
                        </Trigger>
                        <Trigger Property="Text" Value="">
                            <Setter TargetName="PlaceholderTextElement" Property="Visibility" Value="Visible" />
                        </Trigger>

                        <!--  Смена цвета рамки при фокусе  -->
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="BorderElement" Property="BorderBrush" Value="{DynamicResource TextGlowBrush}" />
                            <Setter TargetName="BorderElement" Property="Background" Value="{DynamicResource TextBackground}" />
                            <!--  Или другой цвет  -->
                        </Trigger>

                        <!--  Смена цвета при наведении мыши  -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BorderElement" Property="BorderBrush" Value="{DynamicResource TextGlowBrush}" />
                        </Trigger>

                        <!--  Состояние Disabled (неактивен)  -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="StepCircle" TargetType="Border">
        <Setter Property="Width" Value="32" />
        <Setter Property="Height" Value="32" />
        <Setter Property="CornerRadius" Value="16" />
        <Setter Property="Background" Value="{DynamicResource StepCircleBackground}" />
    </Style>
    <Style
        x:Key="StepCircleDisable"
        BasedOn="{StaticResource StepCircle}"
        TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource StepCircleDisableBackground}" />
    </Style>
    <Style
        x:Key="StepCircleSucces"
        BasedOn="{StaticResource StepCircle}"
        TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource StepCircleSuccesBackground}" />
    </Style>


    <Style x:Key="ServerButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource BorderBlock}" />
        <Setter Property="Foreground" Value="{DynamicResource TitleText}" />
        <Setter Property="FontFamily" Value="AdwaitaSans" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="Padding" Value="15,12" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Margin" Value="0,5" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        CornerRadius="12">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ButtonMouseOverBackground}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ButtonPressedBackground}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect
                    BlurRadius="40"
                    Direction="270"
                    Opacity="0.25"
                    ShadowDepth="8"
                    Color="{DynamicResource ButtonDropShadow}" />
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SidebarMenuButtonStyle" TargetType="Button">
        <Setter Property="Padding" Value="15,8" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontFamily" Value="{DynamicResource AdwaitaSans}" />
        <Setter Property="Margin" Value="15,5,0,5" />
        <Setter Property="Foreground" Value="{DynamicResource TitleText}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <ui:SymbolIcon
                            Margin="0,0,10,0"
                            FontSize="20"
                            Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                            Symbol="{Binding (extensions:SidebarExtensions.IconSymbol), RelativeSource={RelativeSource AncestorType=Button}}" />
                        <TextBlock
                            VerticalAlignment="Center"
                            Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                            Text="{Binding Content, RelativeSource={RelativeSource AncestorType=Button}}" />
                    </StackPanel>
                </DataTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Audio player slider style  -->
    <Style x:Key="AudioSliderStyle" TargetType="Slider">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="Foreground" Value="#df5000" />
        <Setter Property="Height" Value="6" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="SmallChange" Value="1" />
        <Setter Property="LargeChange" Value="10" />
        <Setter Property="IsMoveToPointEnabled" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid>
                        <!--  Track background  -->
                        <Border
                            x:Name="TrackBackground"
                            Height="4"
                            Margin="0,2"
                            Background="#30FFFFFF"
                            CornerRadius="2" />
                        <!--  Track fill (progress)  -->
                        <Border
                            x:Name="TrackFill"
                            Height="4"
                            Margin="0,2"
                            HorizontalAlignment="Left"
                            Background="#df5000"
                            CornerRadius="2" />
                        <!--  Thumb  -->
                        <Border
                            x:Name="Thumb"
                            Width="12"
                            Height="12"
                            Margin="-6,0,0,0"
                            HorizontalAlignment="Left"
                            Background="White"
                            BorderBrush="#df5000"
                            BorderThickness="2"
                            CornerRadius="6"
                            RenderTransformOrigin="0.5,0.5">
                            <Border.RenderTransform>
                                <ScaleTransform ScaleX="0" ScaleY="0" />
                            </Border.RenderTransform>
                        </Border>
                        <!--  Interaction area  -->
                        <Track x:Name="PART_Track" Margin="0,2">
                            <Track.Thumb>
                                <Thumb
                                    Width="20"
                                    Height="20"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="0"
                                    Focusable="False"
                                    OverridesDefaultStyle="True">
                                    <Thumb.Template>
                                        <ControlTemplate>
                                            <Grid Background="Transparent" />
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="Thumb"
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                            To="1"
                                            Duration="0:0:0.15" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="Thumb"
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                            To="1"
                                            Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetName="Thumb"
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
                                            To="0"
                                            Duration="0:0:0.15" />
                                        <DoubleAnimation
                                            Storyboard.TargetName="Thumb"
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
                                            To="0"
                                            Duration="0:0:0.15" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Заголовок секции настроек  -->
    <Style x:Key="HeaderSettings" TargetType="TextBlock">
        <Setter Property="FontSize" Value="22" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TitleText}" />
        <Setter Property="Margin" Value="0,16,0,8" />
    </Style>

    <!--  Settings Sidebar ListBox  -->
    <Style x:Key="SettingsSidebarListBox" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
    </Style>

    <!--  Settings Sidebar Item  -->
    <Style x:Key="SettingsSidebarItem" TargetType="ListBoxItem">
        <Setter Property="Height" Value="36" />
        <Setter Property="Margin" Value="4,1" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Grid>
                        <Border
                            x:Name="Bg"
                            Background="Transparent"
                            CornerRadius="6" />
                        <Border
                            x:Name="Accent"
                            Width="2"
                            Height="16"
                            HorizontalAlignment="Left"
                            Background="#df5000"
                            CornerRadius="1"
                            Visibility="Collapsed" />
                        <ContentPresenter Margin="{TemplateBinding Padding}" VerticalAlignment="Center" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SettingsSidebarItemHover}" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Bg" Property="Background" Value="{DynamicResource SettingsSidebarItemSelected}" />
                            <Setter TargetName="Accent" Property="Visibility" Value="Visible" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--  Settings Sidebar Separator  -->
    <Style x:Key="SettingsSidebarSeparator" TargetType="Separator">
        <Setter Property="Margin" Value="12,4" />
        <Setter Property="Background" Value="{DynamicResource SeparatorBackground}" />
    </Style>

</ResourceDictionary>